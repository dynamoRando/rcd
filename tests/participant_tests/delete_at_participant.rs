#[ignore = "code not written"]
#[test]
pub fn test() {
    unimplemented!()

    /*
        after accepting a contract, the participant should be able to delete a row locally from their perspective.
        at the host, depending on the contract definition will determine if this results in a 'hard' delete where the 
        row reference is actually removed, or if the row reference is updated to show that the row has been removed 
        at the host.
     */
}