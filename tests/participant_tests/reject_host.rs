#[ignore = "code not written"]
#[test]
pub fn test() {
    unimplemented!()

    /*
        after accepting a contract, the participant should be able to reject a contract formerly agreed to.
        for the host, this means that the next command with result in an auth failure.
     */
}